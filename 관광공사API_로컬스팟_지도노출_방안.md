# 관광공사 API 로컬 스팟 지도 노출 방안

## 배경

- 백엔드에 TourAPI 3단계 필터링(법정동 화이트리스트 → 카테고리/cat3 → 랜드마크 블랙리스트+연관관광지 제외) 파이프라인은 구현 완료 (`TourApiService`, `POST /api/spot/import`).
- 지도 뷰포트 조회 API(`GET /api/spot`)와 프론트 뷰포트 필터링·클러스터링 구조도 구현 완료.
- 다만 아직 **DB에 데이터가 없고**, 프론트 지도 탭은 여전히 목업 데이터(`mockSpots`)만 그리는 중 — 실제로 지도에 뜨게 하려면 아래 결정들이 필요함.

---

## 결정 사항 1. 데이터 시딩 시점/방식

| 선택지 | 설명 | 비고 |
|---|---|---|
| **A. 수동 1회 import + 눈으로 검수 (추천)** | `POST /api/spot/import` 한 번 호출 → 결과를 사람이 직접 훑어보고 이상한 항목 제거 | cat3 화이트리스트가 아직 추정치라 초기엔 검증 필요 |
| B. 승인 없이 즉시 자동 반영 | import 즉시 지도에 노출, 별도 검수 없음 | 빠르지만 잘못 걸러진 데이터가 바로 노출될 위험 |
| C. 배치 스케줄러로 주기 수집 | cron 등으로 일 단위 자동 재수집 | 지금 단계에선 과함 (공모전 데모 목적에 비해 인프라 부담↑) |

## 결정 사항 2. 프론트 연결 방식

| 선택지 | 설명 | 비고 |
|---|---|---|
| **A. 뷰포트 콜백을 실 API 호출로 교체 (추천)** | 이미 만들어둔 `onBoundsChanged` 콜백에서 mock 필터링 대신 `GET /api/spot?swLat=...` 호출 | 구조 재사용 가능, HTTP 클라이언트 패키지(`http`) 추가만 필요 |
| B. 앱 시작 시 전체 캐싱 후 클라이언트 필터링 | 최초 1회 전체 로드, 이후 클라이언트에서 뷰포트 필터 | 스팟 수 적을 땐 괜찮지만 데이터 늘어나면 비효율 |
| C. mock과 실 데이터 병행 운영 | 목업은 데모용으로 남기고 실 데이터는 별도 플래그로 전환 | 임시 방편, 장기적으로는 정리 필요 |

## 결정 사항 3. 관광공사 데이터 vs 사용자 등록 스팟 구분

| 선택지 | 설명 | 비고 |
|---|---|---|
| **A. `tourApiId` null 여부로 구분 + UI 배지 (추천)** | 스키마 변경 없이 기존 컬럼만으로 구분, 상세 화면에 "관광공사 제공 정보" 배지 표시 | 가장 단순, 즉시 적용 가능 |
| B. `source` 컬럼 신설 | `tour_api` / `user_submitted` 등 명시적 컬럼 추가 | 향후 출처가 3개 이상으로 늘어나면 유리하지만 지금은 과함 |
| C. 구분 없이 통합 노출 | 출처 표시 없이 동일하게 노출 | 가장 간단하지만 사용자에게 데이터 신뢰도 정보 제공 안 됨 |

## 결정 사항 4. 검수/승인 파이프라인

| 선택지 | 설명 | 비고 |
|---|---|---|
| **A. 검수 없이 3단계 필터만 신뢰 (현재 구조, 추천)** | import된 스팟은 별도 승인 없이 바로 노출 | `spots` 테이블에 status 컬럼 자체가 없어 스키마 변경 불필요 |
| B. status 컬럼 추가해 pending→approved 흐름 | 관리자 승인 후 노출되는 구조 | 스키마 변경 + 관리자 화면 필요, 공모전 범위 대비 과함 |
| C. 수동 1회 검수 후 반자동 운영 | 최초 import만 사람이 확인, 이후는 A와 동일 | 결정1의 A안과 사실상 동일한 접근 |

---

## 추천 조합 (요약)

1. **결정1-A**: TourAPI 키로 `/api/spot/import` 한 번 실행 → 결과 수동 검수
2. **결정2-A**: 프론트 `onBoundsChanged`를 실제 `GET /api/spot` 호출로 교체
3. **결정3-A**: `tourApiId` 유무로 구분, 상세 화면에 배지만 추가
4. **결정4-A**: 별도 승인 파이프라인 없이 3단계 필터 결과를 그대로 신뢰

→ 스키마 변경 없이 기존에 만들어둔 백엔드/프론트 구조를 그대로 재사용해서 가장 빠르게 실 데이터를 지도에 띄울 수 있는 조합.
