# Project guardrails

- Do not modify any file under `coco_backend/`.
- Backend changes are prohibited unless the user explicitly revokes this rule.
- If a frontend request appears to require backend work, explain the cause, why the backend change would be needed, and the proposed direction without editing the backend.

