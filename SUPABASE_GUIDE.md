# Supabase 사용 가이드 (COCO 프로젝트)

작성일: 2026-08-13
구성: Part 1 - Supabase 일반 사용법 (원본 정리본) / Part 2 - COCO 프로젝트 적용 버전 (Spring Boot + Flutter)

---

## Part 1. Supabase 일반 사용법

### 1-1. Supabase란?

오픈소스 Firebase 대안. Postgres 데이터베이스를 중심으로 아래 기능을 한 플랫폼에서 제공한다.

- **Database**: 완전한 Postgres 데이터베이스 (프로젝트마다 1개)
- **Auth**: 이메일/비밀번호, 소셜 로그인(OAuth), 매직링크, 전화번호 인증
- **Storage**: 이미지·파일 저장, RLS 기반 접근 제어
- **Realtime**: DB 변경 사항을 클라이언트로 실시간 push
- **Edge Functions**: Deno 기반 서버리스 함수

### 1-2. 프로젝트 시작하기

1. [supabase.com](https://supabase.com) 가입 → 새 프로젝트 생성 (조직/이름/DB 비밀번호/리전)
2. **Project Settings → API**에서 값 확인
   - `Project URL`
   - `anon public key` (클라이언트용)
   - `service_role key` (서버 전용, **클라이언트에 절대 노출 금지**)
3. Supabase Studio(웹 대시보드)에서 테이블 생성·SQL 실행·Auth 사용자 관리·Storage 버킷 관리 가능

### 1-3. 로컬 개발 (CLI)

Docker 필요. 로컬에 API·DB·Auth·Storage를 그대로 재현한다.

```bash
npm install -g supabase
supabase init
supabase start      # 로컬 스택 실행 (Docker 기반)
supabase status      # 로컬 URL/키 확인
```

로컬 Studio: `http://127.0.0.1:54323`

마이그레이션:

```bash
supabase migration new create_todos_table
supabase db push       # 로컬 → 원격 반영
supabase db pull       # 원격 → 로컬
```

### 1-4. Database & RLS

```sql
create table todos (
  id bigint generated by default as identity primary key,
  task text not null,
  is_complete boolean default false,
  user_id uuid references auth.users default auth.uid(),
  created_at timestamptz default now()
);
```

새 테이블은 기본적으로 RLS(Row Level Security)가 꺼져 있다. **배포 전 반드시 활성화 + 정책 설정.**

```sql
alter table todos enable row level security;

create policy "사용자는 자신의 todo만 조회"
  on todos for select
  using (auth.uid() = user_id);

create policy "사용자는 자신의 todo만 생성"
  on todos for insert
  with check (auth.uid() = user_id);
```

### 1-5. 클라이언트 라이브러리 (JS 예시)

```bash
npm install @supabase/supabase-js
```

```js
import { createClient } from '@supabase/supabase-js'
const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY)

// 조회
const { data, error } = await supabase.from('todos').select('*')
// 삽입
const { error } = await supabase.from('todos').insert({ task: '리포트 작성', user_id: userId })
```

### 1-6. Auth

```js
await supabase.auth.signUp({ email, password })
await supabase.auth.signInWithPassword({ email, password })
await supabase.auth.signInWithOAuth({ provider: 'google' })
await supabase.auth.signOut()
const { data: { session } } = await supabase.auth.getSession()
```

Auth + RLS를 함께 쓰면 `auth.uid()`로 로그인 사용자를 DB 정책에서 바로 참조 가능 (서버 코드 없이도 안전한 접근 제어).

### 1-7. Storage

```js
// 업로드
await supabase.storage.from('avatars').upload('public/user1.png', file)
// public URL
const { data } = supabase.storage.from('avatars').getPublicUrl('public/user1.png')
```

버킷 단위로 Storage 정책(RLS와 유사)을 걸어 접근을 제어한다.

### 1-8. Realtime

```js
supabase
  .channel('todos-changes')
  .on('postgres_changes', { event: '*', schema: 'public', table: 'todos' }, (payload) => console.log(payload))
  .subscribe()
```

### 1-9. Edge Functions (선택)

```bash
supabase functions new hello-world
supabase functions deploy hello-world
```

Deno 런타임, 결제 웹훅·서버 전용 로직에 활용.

### 1-10. 참고 자료

- [Supabase 공식 문서 - Getting Started](https://supabase.com/docs/guides/getting-started)
- [Supabase Local Development](https://supabase.com/docs/guides/local-development/cli/getting-started)
- [Supabase Database 개요](https://supabase.com/docs/guides/database/overview)
- [Supabase JavaScript API Reference](https://supabase.com/docs/reference/javascript/installing)

---

## Part 2. COCO 프로젝트 적용 버전 (Spring Boot + Flutter)

### 2-1. 적용 범위 결정

Supabase는 "어디까지 쓰느냐"에 따라 영향 범위가 완전히 달라진다. 세 가지 옵션을 비교했다.

| | **A. DB 호스팅만** | **B. DB + Storage** | **C. DB + Auth 전환** |
|---|---|---|---|
| 바뀌는 것 | Postgres 접속 정보만 | A + 사진 업로드 로직 | 로그인/회원가입 전체 |
| 기존 코드 영향 | 거의 없음 | Storage 업로드 부분만 신규 | AuthController·JWT·UserRepository 폐기 |
| 작업량 | 매우 적음 | 중간 | 큼 |
| 리스크 | 낮음 | 중간 (Storage 정책 잘못 열면 파일 노출) | 높음 (인증 갈아엎기, RLS로 권한 재설계) |
| 채택 여부 | ✅ 채택 | 보류 (필요해지면 추가) | ❌ 비채택 |

**A안(DB 호스팅만)을 채택.** COCO는 이미 `AuthController`/`AuthService`/`UserRepository` 기반 커스텀 JWT 로그인이 구현돼 있어([[AUTH_API_SPEC.md]] 참고), Supabase Auth로 갈아엎을 이유가 없다. Supabase는 그냥 관리형 Postgres 하나로만 쓴다 — 이전에 검토했던 Render+Neon 조합에서 **Neon 자리에 Supabase를 넣는 것과 동일한 포지션**이라, Neon과 Supabase 둘 다 쓸 필요는 없다.

프로젝트 생성 시 **Enable Data API / Automatically expose new tables / Enable automatic RLS는 전부 끔.** Flutter·Spring Boot 어느 쪽도 supabase-js 등 클라이언트 라이브러리를 쓰지 않고 Spring Boot가 JDBC로 직접 붙기 때문에, Data API(PostgREST 자동 노출 레이어)를 켜두면 Spring Boot의 인증 로직을 우회해서 테이블에 바로 접근 가능한 API가 열려버려 오히려 보안 구멍이 된다.

### 실제 프로젝트 연결 정보 (2026-08-28 생성)

- Organization: `COCO` (Free)
- Region: `ap-northeast-2` (Seoul)
- Transaction pooler host: `aws-0-ap-northeast-2.pooler.supabase.com`
- Pooler username: `postgres.zjtfaolrrxiqfiypssmy`
- DB 비밀번호는 문서에 적지 않음 — 팀원끼리 별도 채널(비밀번호 관리자 등)로 공유하고 각자 로컬 환경변수로만 설정할 것.

### 2-2. Spring Boot 연동 설정

**`application.yml` / `application.properties`**

```yaml
spring:
  datasource:
    url: jdbc:postgresql://<project-id>.pooler.supabase.com:6543/postgres
    username: postgres
    password: ${SUPABASE_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
```

Supabase Dashboard → **Project Settings → Database**의 접속 문자열을 그대로 사용한다.

> **Direct(5432) 대신 Transaction Pooler(6543, PgBouncer)를 쓸 것.**
> Spring Boot의 HikariCP 커넥션 풀과 Direct 연결이 궁합이 안 맞아 드라이버 초기화 실패·타임아웃이 자주 발생한다는 사례가 많다. Pooler URL(`*.pooler.supabase.com:6543`)로 붙이면 안정적이다.

비밀번호에 `#`, `$`, `%` 같은 특수문자가 있으면 URL 인코딩이 필요하다.

**`pom.xml` 의존성 확인**

```xml
<dependency>
  <groupId>org.postgresql</groupId>
  <artifactId>postgresql</artifactId>
</dependency>
```

### 2-3. RLS는 켜지 않는다

Flutter가 Supabase를 직접 호출하지 않고 지금처럼 **Spring Boot REST API만 호출**하는 구조이므로, DB에는 항상 Spring Boot(신뢰된 서버 계정)만 접속한다. Row Level Security는 클라이언트가 DB를 직접 두드릴 때 필요한 방어선이라 이 구조에선 불필요 — 오히려 켜면 JPA 쿼리가 막힐 수 있다.

### 2-4. 스키마 관리 전략

Part 1의 `supabase migration` / `db push`는 Supabase CLI 전용 흐름이라 우리 구조와 맞지 않는다. Spring Boot 쪽에서 스키마를 관리한다.

- **지금(데모 단계)**: `spring.jpa.hibernate.ddl-auto=update`로 편하게 진행
- **이후(정식 운영 고려 시)**: Flyway/Liquibase 마이그레이션으로 전환 권장

### 2-5. 무료 티어 주의사항 — 자동 일시정지

Supabase 무료 프로젝트는 **7일간 API/DB 요청이 없으면 자동 일시정지**된다. 데이터는 남아있지만 재개 전까지 접속이 안 된다.

**공모전 데모 체크리스트에 추가:**
- 심사 발표 며칠 전 반드시 한 번 접속해서 프로젝트를 깨워둘 것
- 또는 GitHub Actions 등으로 주기적 ping 설정해 비활성 상태를 방지

### 2-6. Flutter 쪽은 변경 없음

이 구조에서 Flutter는 Supabase를 전혀 모른다. 지금처럼 Spring Boot REST API만 호출하면 되고, `supabase_flutter` 패키지 설치도 불필요하다.

### 2-7. 향후 확장 옵션 (필요해지면)

피드 감성 사진·프로필 사진처럼 사용자 업로드 이미지가 늘어나면 **B안(Storage)**을 나중에 추가로 검토할 수 있다. 이 경우 Flutter에서 Supabase Storage로 직접 업로드할지, Spring Boot를 경유해 업로드할지부터 결정이 필요하다 (직접 업로드가 더 단순하지만 앱에 `anon key`를 넣고 Storage 정책으로 접근을 제어해야 함).

### 2-8. 참고 자료 (Spring Boot 연동 조사 출처)

- [Supabase Free Tier Paused and Lost Data: What Happened](https://simplebackups.com/blog/supabase-free-tier-paused)
- [Supabase Connection Pooler Deprecating Session Mode on Port 6543](https://supabase.com/changelog/32755-supabase-connection-pooler-deprecating-session-mode-on-port-6543-on-february-28)
- [Supabase Connection String Guide: Ports, Pooling & Errors](https://www.weweb.io/blog/supabase-connection-string-guide-ports-pooling)
- [How I Fixed My Supabase + Spring Boot PostgreSQL Connection Issue](https://medium.com/@hirunmihisara/how-i-finally-fixed-my-supabase-spring-boot-postgresql-connection-issue-after-hours-of-pain-c45f56a5d82b)
- [Connect to your database | Supabase Docs](https://supabase.com/docs/guides/database/connecting-to-postgres)
