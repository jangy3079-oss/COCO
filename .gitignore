.gradle/
build/

# ── Backend (Java/Spring Boot) ───────
coco_backend/.gradle/
coco_backend/build/
coco_backend/out/
coco_backend/*.iml
*.class
*.jar
*.war
.env

# ── Frontend (Flutter) ───────────────
coco_frontend/.dart_tool/
coco_frontend/.flutter-plugins
coco_frontend/.flutter-plugins-dependencies
coco_frontend/build/
coco_frontend/.packages
# pubspec.lock은 앱(패키지 아님)이라 커밋해서 팀원끼리 동일한 의존성 버전 유지 (일부러 추적함)

# ── IDE ──────────────────────────────
.idea/
*.iml
.vscode/
.DS_Store