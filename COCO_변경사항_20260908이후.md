# COCO 변경사항 정리 (마지막 커밋 이후)

기준 커밋: `86379e8` "Merge branch 'harry-dev'" (2026-09-08)
정리 시점: 2026-09-12

현재 이 내용은 전부 **로컬 워킹 디렉토리에만 있는 미커밋 변경사항**이다 (`git status` 기준 39개 파일 수정, 다수 신규 파일 미추적). 친구 브랜치를 병합하기 전에 먼저 커밋/푸시가 필요하다.

---

## 1. 인증 (JWT)

- `SecurityConfig`에 `JwtAuthenticationFilter`를 `UsernamePasswordAuthenticationFilter` 앞단에 추가. 아직 엔드포인트 접근 제한(permitAll)은 그대로지만, 유효한 토큰이 있으면 `SecurityContext`에 로그인한 사용자(userId)를 심어줘서 컨트롤러가 "누가 요청했는지" 알 수 있게 됨.
- `JwtTokenProvider`에 `getUserId(token)` 추가 — 토큰 subject에서 userId 추출, 서명 오류/만료는 예외로 던지고 필터에서 무시 처리.
- 신규 `JwtAuthenticationFilter.java` (미추적 파일).
- 프론트: `login_screen.dart`, `signup_screen.dart` 수정 + 신규 `auth_repository.dart`, `core/network/` (Dio 클라이언트) 추가 — 실제 로그인/토큰 저장 연동.

## 2. 스팟 / 지도 (백엔드)

- **Spot 엔티티 확장**: `kakaoPlaceId`(카카오 로컬 소스 dedupe용), `description`(소개글), `isLocalPick`(팀이 수동 지정하는 로컬 픽 여부, 기본 false) 컬럼 추가.
- **SpotResponse 확장**: `description`, `isLocalPick`, `trending` 필드 추가. 지도 핀에서 **색(isLocalPick)**과 **크기(trending: 게시물 수·좋아요 기준)**를 서로 다른 신호로 분리.
- `KakaoLocalService` 신규 구현 — 부산 15개 구 전역(약 200개 동) 대상으로 카카오 로컬 키워드 검색(AT4/FD6/CE7/CT1 카테고리 + 공원 별도 조회)해서 스팟 후보 수집. `TourApiService`는 남포동/영주동/초량동 3개 동 + 화이트리스트/블랙리스트로 좁게 수집하는 반면, 이쪽은 커버리지를 크게 넓히는 용도.
- `TourApiService` CAT3_WHITELIST를 실제 API 키로 검증한 값으로 확정, 지역코드 오류 수정(중구/동구 시군구 코드가 잘못 들어가 있어서 import 0건이었던 버그 수정).
- `SpotService`: 뷰포트 조회(`findInViewport`)에 카테고리 필터, 트렌딩 판정 로직(`isTrending`, 게시물 수 3건 또는 좋아요 합 20 이상) 추가 — 피드 인기 배지와 공유해서 쓸 수 있게 `public static`으로 분리. 검색(`search`) 결과 20건 상한 추가.
- `SpotController`에 뷰포트 조회 시 `category`/`locale` 파라미터, 카카오 로컬 수집 트리거(`POST /api/spot/import/kakao`) 엔드포인트 추가.

## 3. 피드 (백엔드)

- `FeedPostRequest.imageUrl`을 필수(`@NotBlank`)에서 선택으로 변경, `spotId`는 필수(`@NotNull`)로 명시 — 사진 없이도 글+스팟 태그만으로 작성 가능하게.
- **이미지 업로드**: 신규 `FileStorageService`(로컬 디스크 저장, jpg/png/webp/gif만 허용) + `FeedController`에 `POST /api/feed/images` 추가. `CorsConfig`에 `/uploads/**` 정적 리소스 매핑, `application.yml`에 멀티파트 용량 제한(5MB) 추가.
- **좋아요 토글**: `FeedPostLikeRepository`에 사용자별 좋아요 조회/일괄 조회 쿼리 추가, `FeedPost`에 `increaseLike()/decreaseLike()`, 신규 `LikeToggleResponse` DTO, `FeedController`에 `POST /api/feed/{id}/like` 추가.
- **댓글 CRUD**: 신규 `FeedCommentRequest`/`FeedCommentResponse` DTO, `FeedCommentRepository`에 게시물별 조회 쿼리, `FeedController`에 `GET/POST /api/feed/{id}/comments` 추가.
- `FeedPostResponse`에 `liked`(현재 사용자가 좋아요했는지) 필드 추가.
- `FeedService`: 목록 조회 시 스팟별 트렌딩 판정 + 사용자별 좋아요 여부를 한 번에 계산해서 응답에 포함하도록 대폭 확장.

## 4. 피드 (프론트)

- `FeedPost` 모델에 `liked` 필드, 신규 `FeedCommentDto` 추가.
- `FeedRepository`에 `uploadImage`, `toggleLike`, `fetchComments`, `createComment` 추가.
- `feed_composer_screen.dart`: 사진 첨부 단계를 실제 갤러리 선택(`image_picker`) + 업로드로 구현 (기존엔 UI만 있고 실제 동작 없었음).
- `feed_screen.dart` / `feed_post_detail_screen.dart`: 좋아요 버튼 실제 API 연동(낙관적 업데이트 + 실패 시 롤백), 댓글 목록/작성 실 API 연동, 업로드된 이미지 `Image.network`로 렌더링.
- `feed_mock_data.dart`: 백엔드 `likeCount`(현재 사용자 좋아요 포함한 총합)와 프론트 `likes`(그걸 제외한 값) 사이 중복 카운트 안 되도록 변환 로직 보정.

## 5. 지도 탭 UX (프론트)

- 지도 진입 시 위치 상태, 뷰포트 필터, 카카오맵 JS interop 위젯(`kakao_map_view_web.dart`/`_stub.dart`) 관련 마커에 `subtitle`(카테고리) 필드 추가.
- **핀 탭 → 정보 말풍선(콜아웃)**: 핀을 눌러도 바로 상세로 이동하지 않고, 이름/카테고리가 담긴 말풍선이 먼저 뜨고 "자세히 보기"를 한 번 더 눌러야 상세로 이동 (`web/index.html`의 `cocoShowCallout`/`cocoHideCallout` 신규 구현).
- **검색 결과 탭 동작 변경**: 기존엔 검색 결과를 누르면 바로 상세로 이동했는데, 이제는 그 스팟의 핀 위치로 지도만 이동(`_openSearchResult`). 상세 진입은 위 콜아웃 흐름을 거치도록 통일.
- `map_screen.dart`, `map_mock_data.dart`, `route_builder_screen.dart`, `spot_detail_screen.dart`, `my_saved_screen.dart` 등 실제 DB 스팟(`db-` 접두어) 연동 관련 보완.

## 6. 기타

- `pubspec.yaml`/`pubspec.lock`: `image_picker` 등 의존성 추가.
- `.env.example`(백엔드)에 신규 환경변수 항목 추가.

---

## 참고 — 정리가 필요한 항목

- **미추적 잡파일 다수**: `application.backup`, `coco_frontend/.flutter-plugins-dependencies 2`, `my_posts_screen 2.dart`, `coco_mark 2.dart` 등 macOS 중복 저장본으로 보이는 파일들이 있음. 커밋 전에 지울지 확인 필요.
- 프로젝트 루트에 새 한글 파일명 문서(`관광공사API_로컬스팟_지도노출_방안.md`)가 미추적 상태로 있음 — 커밋 대상인지 확인 필요.
- `git status` 확인 중 `.git/index.lock` 관련 경고가 한 번 떴음(다른 git 프로세스가 잠깐 잡고 있었던 것으로 보임) — 커밋 전에 다시 한번 `git status`로 정상 동작 확인 권장.
